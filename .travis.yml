language: python
python:
    - "2.7"
dist: xenial
os: linux

# Cache PlatformIO packages using Travis CI container-based infrastructure
cache:
    directories:
        - "~/.platformio"

env:
    - PLATFORMIO_CI_SRC=src

install:
    - pip install -U platformio
    - platformio update

script:
    - platformio ci --board=d1_mini_pro
